# TASK: Prepare a simple calculator that will perform the four standard operations. Without using (if/else).

## Example:

const selectedOperation = "multiply";
console.log(calculate(6, 3, operations[selectedOperation])); // 18
